{
  "name": "task-app-clean-updated",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm:dev-server\" \"npm:dev-client\"",
    "dev-server": "cd server && npm install && npm run dev",
    "dev-client": "cd client && npm install && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "mongoose": "^8.18.0"
  }
}
